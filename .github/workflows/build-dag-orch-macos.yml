name: Build DAG Orchestrator - macOS

# When this runs:
#   * Push a tag matching dag-v*.*.*  (e.g. dag-v1.0.1) — produces a release-ready build.
#   * Manual trigger from the Actions tab — for ad-hoc test builds.
#
# What it does:
#   1. Spins up an Apple-silicon macOS-14 runner.
#   2. Installs Python + project deps + PyInstaller.
#   3. Builds Cogniflow Orchestrator.app via cogniflow-orchestrator.spec.
#   4. Uploads the .app as a downloadable workflow artifact.
#
# What it intentionally does NOT do (yet):
#   * Code-signing or Apple notarization. The first launch will trigger
#     Gatekeeper's "developer cannot be verified" dialog. Same workaround
#     as the UI: right-click → Open the first time. Add the APPLE_ID /
#     APPLE_TEAM_ID / APPLE_APP_PASSWORD secrets and wire in `codesign` +
#     `xcrun notarytool` after the build job to remove the warning.

on:
  push:
    tags:
      - "dag-v*.*.*"
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    defaults:
      run:
        working-directory: dag/cogniflow-orchestrator

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: dag/cogniflow-orchestrator/requirements.txt

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build with PyInstaller
        run: pyinstaller --noconfirm --clean cogniflow-orchestrator.spec

      # The spec emits both the one-folder bundle and the .app wrapper.
      # We zip the .app so GitHub Actions preserves Unix file permissions
      # (the upload-artifact action otherwise loses the executable bit).
      - name: Zip the .app for upload
        run: |
          cd dist
          ditto -c -k --sequesterRsrc --keepParent "Cogniflow Orchestrator.app" "Cogniflow-Orchestrator-DAG-macOS.zip"
          ls -la

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: cogniflow-orchestrator-dag-macos
          path: dag/cogniflow-orchestrator/dist/Cogniflow-Orchestrator-DAG-macOS.zip
          if-no-files-found: error
          retention-days: 30

      - name: Attach to GitHub Release
        if: startsWith(github.ref, 'refs/tags/dag-v')
        uses: softprops/action-gh-release@v2
        with:
          files: dag/cogniflow-orchestrator/dist/Cogniflow-Orchestrator-DAG-macOS.zip
          generate_release_notes: true
